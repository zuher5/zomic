# FastAPI Cloud packaging: hanya file runtime yang dibutuhkan.
# (app.py + komiku_web.py + web/ + pyproject.toml/requirements.txt di-upload)
# .gitignore tetap berlaku; aturan di file ini mengambil alih (precedence).

# api/ = Vercel entrypoint — tidak dipakai FastAPI Cloud
api/
scripts/  # dev tools lokal (smoke test / benchmark), tidak dibutuhkan runtime

# Artefak lokal
tests/__pycache__/
*.log
*.zip
*Zone.Identifier
